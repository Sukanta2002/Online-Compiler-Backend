{
  "name": "online-compiler-backend",
  "version": "1.0.0",
  "description": "compiler backend ",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node dotenv/config --experimental-json-modules src/index.js",
    "dev": "nodemon --watch src -r dotenv/config --experimental-json-modules src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
